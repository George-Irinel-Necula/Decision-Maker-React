export default function Interact({play}){
    return(
        <div style={{padding:"1rem"}}>
            <h1>Merg azi la facultate?</h1>
            <button onClick={play}>Gamble</button>
        </div>
    )
}